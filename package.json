{
  "name": "rollover-plus",
  "version": "2.2.0",
  "private": true,
  "description": "Roll unfinished tasks between daily notes.",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "eslint main.js manifest.json --max-warnings 0",
    "check": "node --check main.js && npm run lint && npm test"
  },
  "devDependencies": {
    "eslint": "^10.9.1",
    "eslint-plugin-obsidianmd": "^0.4.2",
    "moment": "^2.30.1",
    "typescript-eslint": "^8.35.1"
  }
}
